name: CI/CD Pipeline

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code Repository
        uses: actions/checkout@v4

      - name: Setup Node.js Environment
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install Dependencies
        run: npm install

      - name: Compile and Validate Midnight Compact Smart Contracts
        run: npm run compile:compact

      - name: TypeScript Type Checking
        run: npx tsc --noEmit

      - name: Run Vitest Test Suite
        run: npm run test

      - name: Execute Devnet Contract & ZK Circuit Test Suite
        run: npm run test:devnet

      - name: Build Vite Production Web App
        run: npm run build
